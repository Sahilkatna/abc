{%
  set classes = [
    'region--' ~ region|clean_class,
  ]
%}
{% if content %}
  <div class="hero-wrap-bottom sub-title-header">
      <div{{ attributes.addClass(classes).setAttribute('id', 'hero-bottom') }}>
          {{ content }}
      </div>
  </div>
{% endif %}