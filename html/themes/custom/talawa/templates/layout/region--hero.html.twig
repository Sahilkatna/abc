{%
  set classes = [
    'region--' ~ region|clean_class,
    'container',
  ]
%}
{% if content %}
  <div class="hero-wrapper sub-title-header">
      <div{{ attributes.addClass(classes).setAttribute('id', 'hero') }}>
          {{ content }}
      </div>
  </div>
{% endif %}